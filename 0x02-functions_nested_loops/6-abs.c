#include "main.h"
/**
 * _abs - Computing the absolute vakues of an integer.
 * Return: The value of the integer.
 * @n: The integer computed.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);



}
