#include "main.h"
/**
 * _isalpha - Checking for alphabetic characters.
 * Return: 1 if its an upper case otherwise return 0.
 * @c: the char to be checked.
 */

int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z')
		|| (c >= 'A' && C <= 'Z')
			return (1);
	else
		return (0);


}
