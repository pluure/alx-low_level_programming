#include "main.h"
#include<stdio.h>
/**
 * _isupper - checks for uppercase character.
 * @c : character to be used for checks.
 * Return: 1 if character is upper 0 if character is lower..
 *
 */
int _isupper(int c)
{

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}




}
