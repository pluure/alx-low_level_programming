#include "main.h"
#include <stdio.h>
/**
 * main - function that prints name.
 * @argc: number of arguments
 * @argv: name in array
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);


}
