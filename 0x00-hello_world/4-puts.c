#include <stdio.h>
int main(void)
/*
 * using void because we are not accepting any inputs
 * retun(0) for code to function
 */
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
